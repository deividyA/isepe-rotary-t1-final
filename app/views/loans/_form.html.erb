<%= form_with(model: loan) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
        <% loan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :id_item, class: "form-label" %>
    <%= form.number_field :id_item, class:"form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :id_people, class: "form-label" %>
    <%= form.number_field :id_people, class:"form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :lending_date, class: "form-label" %>
    <%= form.date_field :lending_date, class:"form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :return_date, class: "form-label" %>
    <%= form.date_field :return_date, class:"form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :reservation_date, class: "form-label" %>
    <%= form.date_field :reservation_date, class:"form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>